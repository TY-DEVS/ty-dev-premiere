[variables]
NITRO_PRESET = "node-server"
PORT = "3000"

[phases.install]
cmds = ["npm install --fetch-retries=5 --fetch-retry-mintimeout=20000 --fetch-retry-maxtimeout=120000"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node .output/server/index.mjs"
